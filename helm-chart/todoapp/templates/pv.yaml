apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ .Chart.Name }}-pv

spec:
  capacity:
    storage: {{ .Values.persistentVolume.capacity }}
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/data"